import pandas as pd

# Organizes features/targets into X and Y dataframes referenced by timestamp and location
# Adds wind speed, MAP, MAT and species data to each feature entry
# Removes extraneous/irrelevant data
# Removes entries with missing data
# Splits data into training and validation sets

